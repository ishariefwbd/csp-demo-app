version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto17

  build:
    commands:
      - echo "=== Backend PR - compile and test ==="
      - mvn -f backend/pom.xml test --no-transfer-progress
      - echo "=== All backend tests passed ==="
