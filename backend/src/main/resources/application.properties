server.port=8080
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

app.version=${APP_VERSION:1.0.0-local}
app.env=${APP_ENV:local}

spring.application.name=csp-demo
